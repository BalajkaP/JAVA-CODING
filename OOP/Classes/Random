doubles()
Vrací efektivně neomezený proud pseudonáhodných dvojkových hodnot, z nichž každá je mezi nulou (včetně) a jedničkou (výlučně).

doubles(double randomNumberOrigin, double randomNumberBound)
Vrací efektivně neomezený proud pseudonáhodných dvojnásobných hodnot, z nichž každá odpovídá zadanému původu (včetně) a ohraničení (výlučně).

doubles(long streamSize)
Vrací proud vytvářející zadaný počet pseudonáhodných dvojnásobných hodnot o velikosti streamSize, z nichž každá se pohybuje mezi nulou (včetně) a jedničkou (výlučně).

doubles(long streamSize, double randomNumberOrigin, double randomNumberBound)
Vrací proud produkující zadaný počet pseudonáhodných dvojnásobných hodnot o velikosti streamSize, z nichž každá odpovídá zadanému původu (včetně) a ohraničení (výlučně).

ints()
Vrací efektivně neomezený proud pseudonáhodných hodnot int.

ints(int randomNumberOrigin, int randomNumberBound)
Vrací efektivně neomezený proud pseudonáhodných hodnot int, z nichž každá odpovídá zadanému původu (včetně) a omezení (výlučně).

ints(long streamSize)
Vrací proud vytvářející zadaný počet pseudonáhodných hodnot int o velikosti streamSize.

ints(long streamSize, randomNumberOrigin, int randomNumberBound)
Vrací proud produkující zadaný počet pseudonáhodných hodnot int o velikosti streamSize, z nichž každá odpovídá zadanému původu (včetně) a ohraničení (výlučně).

longs()
Vrací efektivně neomezený proud pseudonáhodných dlouhých hodnot.

longs(long streamSize)
Vrací proud vytvářející zadaný počet pseudonáhodných dlouhých hodnot o velikosti streamSize.

longs(long randomNumberOrigin, long randomNumberBound)
Vrací efektivně neomezený proud pseudonáhodných dlouhých hodnot, z nichž každá odpovídá zadanému původu (včetně) a omezení (výlučně).

longs(long streamSize, long randomNumberOrigin, long randomNumberBound)
Vrací proud vytvářející zadaný počet pseudonáhodných dlouhých proudů o velikosti streamSize, z nichž každý odpovídá zadanému původu (včetně) a ohraničení (výlučně).

next(int bits)
Generuje další pseudonáhodné číslo.

nextBoolean()
Vrací další pseudonáhodnou, rovnoměrně rozloženou logickou hodnotu z posloupnosti tohoto generátoru náhodných čísel.

nextBytes(byte[] bytes)
Generuje náhodné bajty a umístí je do pole bajtů zadaného uživatelem.

nextDouble()
Vrací další pseudonáhodnou, rovnoměrně rozdělenou dvojnásobnou hodnotu mezi 0,0 a 1,0 z posloupnosti tohoto generátoru náhodných čísel.

nextFloat()
Vrací další pseudonáhodnou, rovnoměrně rozdělenou hodnotu float mezi 0,0 a 1,0 ze sekvence tohoto generátoru náhodných čísel.

nextGaussian()
Vrací další pseudonáhodnou, Gaussovsky ("normálně") rozdělenou dvojnásobnou hodnotu se střední hodnotou 0,0 a směrodatnou odchylkou 1,0 z posloupnosti tohoto generátoru náhodných čísel.

nextInt()
Vrací další pseudonáhodnou, rovnoměrně rozdělenou hodnotu int z posloupnosti tohoto generátoru náhodných čísel.

nextInt(int bound)
Vrací pseudonáhodnou, rovnoměrně rozdělenou hodnotu int mezi 0 (včetně) a zadanou hodnotou (výlučně), získanou z posloupnosti tohoto generátoru náhodných čísel.

nextLong()
Vrací další pseudonáhodnou, rovnoměrně rozdělenou dlouhou hodnotu z posloupnosti tohoto generátoru náhodných čísel.

setSeed(long seed)
Nastaví osivo tohoto generátoru náhodných čísel pomocí jediného dlouhého osiva.


Methods inherited from class java.lang.Object
clone, equals, finalize, getClass, hashCode, notify, notifyAll, toString, wait, wait, wait
